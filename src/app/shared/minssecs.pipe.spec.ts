import { MinssecsPipe } from '../topic/minssecs.pipe'

describe('MinssecsPipe', () => {
    it('create an instance', () => {
        const pipe = new MinssecsPipe()
        expect(pipe).toBeTruthy()
    })
})
